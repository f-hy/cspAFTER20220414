#include <bits/stdc++.h>
using namespace std;

const int maxn = 1e2;  // n>=2; 1<=k<=m<=1e2
const int maxr = 1e8;  // 1<=r<=1e8
int n, m, k, r;
struct status {
  long long x, y;
  int step, kcount;
};
int main() {
  ios::sync_with_stdio(false);
  cin >> n >> m >> k >> r;
  return 0;
}